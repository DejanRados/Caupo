<UserControl x:Class="Caupo.UserControls.FloatingMenuBar"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignWidth="280"
             d:DesignHeight="500"
             HorizontalAlignment="Stretch"
             VerticalAlignment="Top">

    <UserControl.Resources>

        <!-- Glavna stavka FloatingMenuBar menija. -->
        <Style x:Key="FloatingMenuButtonStyle" TargetType="Button">
            <Setter Property="Height" Value="45"/>
            <Setter Property="Padding" Value="14,0,10,0"/>
            <Setter Property="Margin" Value="0"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{DynamicResource GlobalFontColor}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource GlobalFontColor}"/>
            <Setter Property="BorderThickness" Value="0,0,0,1"/>
            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>

            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">

                        <Border x:Name="ButtonBorder"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                Opacity="0.85">

                            <ContentPresenter Margin="{TemplateBinding Padding}"
                                              HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                              VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>

                        </Border>

                        <ControlTemplate.Triggers>

                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Opacity" Value="1"/>
                            </Trigger>

                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Opacity" Value="0.65"/>
                            </Trigger>

                        </ControlTemplate.Triggers>

                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Podstavka otvorenog menija. -->
        <Style x:Key="FloatingSubMenuButtonStyle" TargetType="Button">
            <Setter Property="Height" Value="40"/>
            <Setter Property="Margin" Value="0"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{DynamicResource GlobalFontColor}"/>
            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>

            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">

                        <Grid Background="{DynamicResource GlobalBackgroundColor}">

                            <!-- Blaga pozadina odvaja podmeni od glavnog menija. -->
                            <Border x:Name="SubMenuBackground"
                                    Background="{DynamicResource GlobalFontColor}"
                                    Opacity="0.08"/>

                            <Grid Margin="28,0,10,0">

                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="18"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>

                                <!-- Mali kružić ispred svake podstavke. -->
                                <Ellipse Width="5"
                                         Height="5"
                                         Fill="{DynamicResource GlobalFontColor}"
                                         HorizontalAlignment="Left"
                                         VerticalAlignment="Center"
                                         Opacity="0.65"/>

                                <ContentPresenter Grid.Column="1"
                                                  HorizontalAlignment="Left"
                                                  VerticalAlignment="Center"/>

                            </Grid>

                        </Grid>

                        <ControlTemplate.Triggers>

                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="SubMenuBackground" Property="Opacity" Value="0.16"/>
                            </Trigger>

                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="SubMenuBackground" Property="Opacity" Value="0.24"/>
                            </Trigger>

                        </ControlTemplate.Triggers>

                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

    </UserControl.Resources>

    <!-- Glavni okvir FloatingMenuBara. -->
    <Border Background="{DynamicResource GlobalBackgroundColor}"
            BorderBrush="{DynamicResource GlobalFontColor}"
            BorderThickness="1"
            CornerRadius="8">

        <StackPanel>

            <!-- ================================================= -->
            <!-- NASLOV / DRAG POVRŠINA                            -->
            <!-- ================================================= -->

            <Border x:Name="TitleBar"
                    Height="64"
                    Background="{DynamicResource GlobalBackgroundColor}"
                    BorderBrush="{DynamicResource GlobalFontColor}"
                    BorderThickness="0,0,0,1"
                    CornerRadius="7,7,0,0"
                    Cursor="SizeAll"
                    MouseLeftButtonDown="TitleBar_MouseLeftButtonDown"
                    MouseMove="TitleBar_MouseMove"
                    MouseLeftButtonUp="TitleBar_MouseLeftButtonUp">

                <Grid>

                    <!-- Blago izdvojena površina headera. -->
                    <Border Background="{DynamicResource GlobalFontColor}"
                            CornerRadius="7,7,0,0"
                            Opacity="0.10"/>

                    <Grid Margin="14,7,12,7">

                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <!-- Naslov. -->
                        <TextBlock Grid.Row="0"
                                   Grid.Column="0"
                                   Text="Grupno uređivanje"
                                   Foreground="{DynamicResource GlobalFontColor}"
                                   FontSize="15"
                                   FontWeight="SemiBold"
                                   VerticalAlignment="Center"/>

                        <!-- Broj selektovanih artikala. -->
                        <TextBlock x:Name="SelectedCountText"
                                   Grid.Row="3"
                                   Grid.Column="1"
                                   Text="Selektovano: 0"
                                   Foreground="{DynamicResource GlobalFontColor}"
                                   FontSize="12"
                                   VerticalAlignment="Center"
                                   Margin="10,0,0,0"/>

                        <!-- Kratko objašnjenje. -->
                        <TextBlock Grid.Row="1"
                                   Grid.Column="0"
                                   Grid.ColumnSpan="2"
                                   Text="Promijenite podatke za označene artikle"
                                   Foreground="{DynamicResource GlobalFontColor}"
                                   FontSize="11"
                                   Opacity="0.70"
                                   VerticalAlignment="Center"
                                   TextTrimming="CharacterEllipsis"/>

                    </Grid>

                </Grid>

            </Border>
            <!-- ================================================= -->
            <!-- CIJENA                                           -->
            <!-- ================================================= -->


            <Button x:Name="PriceButton" Style="{StaticResource FloatingMenuButtonStyle}" Click="PriceButton_Click">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="25"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="Postavi cijenu" VerticalAlignment="Center"/>
                    <TextBlock Grid.Column="1" Text="&#xE70D;" Foreground="{DynamicResource GlobalFontColor}" FontFamily="Segoe MDL2 Assets" FontSize="12" HorizontalAlignment="Center" VerticalAlignment="Center" RenderTransformOrigin="0.5,0.5">
                        <TextBlock.RenderTransform>
                            <RotateTransform x:Name="PriceArrowTransform"/>
                        </TextBlock.RenderTransform>
                    </TextBlock>
                </Grid>
            </Button>

            <StackPanel x:Name="PricePanel" Background="{DynamicResource GlobalBackgroundColor}" Visibility="Collapsed">
                <TextBlock Text="Unesite željenu cijenu za selektovane artikle:" Foreground="{DynamicResource GlobalFontColor}" TextWrapping="Wrap" Margin="12,10,12,6"/>

                <Grid Margin="12,0,12,10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="2*"/>
                        <ColumnDefinition Width="8"/>
                        <ColumnDefinition Width="1*"/>
                    </Grid.ColumnDefinitions>

                    <TextBox x:Name="PriceTextBox" Style="{DynamicResource TextBoxCustom}"  Grid.Column="0" Height="40" Padding="8,0" FontSize="15" VerticalContentAlignment="Center" KeyDown="PriceTextBox_KeyDown"/>

                    <Button Grid.Column="2" Content="OK" Height="40" Style="{StaticResource SecondaryButtonStyle}" Click="PriceOKButton_Click"/>
                </Grid>
            </StackPanel>
            <!-- ================================================= -->
            <!-- VRSTA                                             -->
            <!-- ================================================= -->

            <Button x:Name="TypeButton"
                    Style="{StaticResource FloatingMenuButtonStyle}"
                    Click="TypeButton_Click">

                <Grid>

                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="25"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Text="Postavi vrstu" VerticalAlignment="Center"/>

                    <TextBlock x:Name="TypeArrow"
                               Grid.Column="1"
                               Text="&#xE70D;"
                               Foreground="{DynamicResource GlobalFontColor}"
                               FontFamily="Segoe MDL2 Assets"
                               FontSize="12"
                               HorizontalAlignment="Center"
                               VerticalAlignment="Center"
                               RenderTransformOrigin="0.5,0.5">

                        <TextBlock.RenderTransform>
                            <RotateTransform x:Name="TypeArrowTransform"/>
                        </TextBlock.RenderTransform>

                    </TextBlock>

                </Grid>

            </Button>

            <StackPanel x:Name="TypePanel"
                        Background="{DynamicResource GlobalBackgroundColor}"
                        Visibility="Collapsed"/>

            <!-- ================================================= -->
            <!-- KATEGORIJA                                        -->
            <!-- ================================================= -->

            <Button x:Name="CategoryButton"
                    Style="{StaticResource FloatingMenuButtonStyle}"
                    Click="CategoryButton_Click">

                <Grid>

                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="25"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Text="Postavi kategoriju" VerticalAlignment="Center"/>

                    <TextBlock Grid.Column="1"
                               Text="&#xE70D;"
                               Foreground="{DynamicResource GlobalFontColor}"
                               FontFamily="Segoe MDL2 Assets"
                               FontSize="12"
                               HorizontalAlignment="Center"
                               VerticalAlignment="Center"
                               RenderTransformOrigin="0.5,0.5">

                        <TextBlock.RenderTransform>
                            <RotateTransform x:Name="CategoryArrowTransform"/>
                        </TextBlock.RenderTransform>

                    </TextBlock>

                </Grid>

            </Button>

            <StackPanel x:Name="CategoryPanel"
                        Background="{DynamicResource GlobalBackgroundColor}"
                        Visibility="Collapsed"/>

            <!-- ================================================= -->
            <!-- JEDINICA MJERE                                    -->
            <!-- ================================================= -->

            <Button x:Name="UnitButton"
                    Style="{StaticResource FloatingMenuButtonStyle}"
                    Click="UnitButton_Click">

                <Grid>

                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="25"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Text="Postavi jedinicu mjere" VerticalAlignment="Center"/>

                    <TextBlock Grid.Column="1"
                               Text="&#xE70D;"
                               Foreground="{DynamicResource GlobalFontColor}"
                               FontFamily="Segoe MDL2 Assets"
                               FontSize="12"
                               HorizontalAlignment="Center"
                               VerticalAlignment="Center"
                               RenderTransformOrigin="0.5,0.5">

                        <TextBlock.RenderTransform>
                            <RotateTransform x:Name="UnitArrowTransform"/>
                        </TextBlock.RenderTransform>

                    </TextBlock>

                </Grid>

            </Button>

            <StackPanel x:Name="UnitPanel"
                        Background="{DynamicResource GlobalBackgroundColor}"
                        Visibility="Collapsed"/>

            <!-- ================================================= -->
            <!-- PORESKA STOPA -->
            <!-- ================================================= -->

            <Button x:Name="TaxButton" Style="{StaticResource FloatingMenuButtonStyle}" Click="TaxButton_Click">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="25"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Text="Postavi poresku stopu" VerticalAlignment="Center"/>

                    <TextBlock Grid.Column="1" Text="&#xE70D;" Foreground="{DynamicResource GlobalFontColor}" FontFamily="Segoe MDL2 Assets" FontSize="12" HorizontalAlignment="Center" VerticalAlignment="Center" RenderTransformOrigin="0.5,0.5">
                        <TextBlock.RenderTransform>
                            <RotateTransform x:Name="TaxArrowTransform"/>
                        </TextBlock.RenderTransform>
                    </TextBlock>
                </Grid>
            </Button>

            <StackPanel x:Name="TaxPanel" Background="{DynamicResource GlobalBackgroundColor}" Visibility="Collapsed"/>


            <!-- ================================================= -->
            <!-- Normativ -->
            <!-- ================================================= -->
            <Button x:Name="NormativButton" Style="{StaticResource FloatingMenuButtonStyle}" Click="NormativButton_Click">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="25"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Text="Postavi normativ" VerticalAlignment="Center"/>

                    <TextBlock Grid.Column="1" Text="&#xE70D;" Foreground="{DynamicResource GlobalFontColor}" FontFamily="Segoe MDL2 Assets" FontSize="12" HorizontalAlignment="Center" VerticalAlignment="Center" RenderTransformOrigin="0.5,0.5">
                        <TextBlock.RenderTransform>
                            <RotateTransform x:Name="NormativArrowTransform"/>
                        </TextBlock.RenderTransform>
                    </TextBlock>
                </Grid>
            </Button>

            <StackPanel x:Name="NormativPanel" Background="{DynamicResource GlobalBackgroundColor}" Visibility="Collapsed"/>
            <!-- ================================================= -->
            <!-- PNP -->
            <!-- ================================================= -->
            <Button x:Name="ConsumptionTaxButton" Style="{StaticResource FloatingMenuButtonStyle}" Click="ConsumptionTaxButton_Click">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="25"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="Porez na potrošnju" VerticalAlignment="Center"/>
                    <TextBlock Grid.Column="1" Text="&#xE70D;" Foreground="{DynamicResource GlobalFontColor}" FontFamily="Segoe MDL2 Assets" FontSize="12" HorizontalAlignment="Center" VerticalAlignment="Center" RenderTransformOrigin="0.5,0.5">
                        <TextBlock.RenderTransform>
                            <RotateTransform x:Name="ConsumptionTaxArrowTransform"/>
                        </TextBlock.RenderTransform>
                    </TextBlock>
                </Grid>
            </Button>

            <StackPanel x:Name="ConsumptionTaxPanel" Background="{DynamicResource GlobalBackgroundColor}" Visibility="Collapsed"/>


            <!-- ================================================= -->
            <!-- Brisanje -->
            <!-- ================================================= -->
            <Button Style="{StaticResource FloatingMenuButtonStyle}" Click="DeleteSelectedButton_Click">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="25"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="Izbriši označene" VerticalAlignment="Center"/>
                    <TextBlock Grid.Column="1" Text="&#xE74D;" Foreground="{DynamicResource GlobalFontColor}" FontFamily="Segoe MDL2 Assets" FontSize="15" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </Grid>
            </Button>

           

            <!-- ================================================= -->
            <!-- ZAVRŠETAK GRUPNOG UREĐIVANJA                      -->
            <!-- ================================================= -->

            <Button x:Name="FinishButton"
                    Content="Završi"
                    Style="{StaticResource CancelButtonStyle}"
                    Height="44"
                    Margin="10"
                    Click="FinishButton_Click"/>

        </StackPanel>

    </Border>

</UserControl>