<UserControl x:Class="Caupo.Views.FoodInPage"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:system="clr-namespace:System;assembly=mscorlib"
        xmlns:local="clr-namespace:Caupo.ViewModels"
        xmlns:loc="clr-namespace:Caupo"
         xmlns:syncfusionskin ="clr-namespace:Syncfusion.SfSkinManager;assembly=Syncfusion.SfSkinManager.WPF" 
         xmlns:svg="http://sharpvectors.codeplex.com/svgc/" xmlns:Caupo="clr-namespace:Caupo"
        mc:Ignorable="d"
        UseLayoutRounding="True"
          >

    <Grid>
        <!-- Header -->
        <Grid HorizontalAlignment="Stretch" VerticalAlignment="Top" Height="55" Background="#323232" >
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="0.2*"/>
            </Grid.ColumnDefinitions>
            <Image Source="pack://application:,,,/Images/dsoftlogosmall.png" Width="50" Height="50" Grid.Column="0" HorizontalAlignment="Left" Margin="20,0,0,0"/>
            <Label x:Name="lblApp" Content="Caupo - Prijem namirnica" Foreground="#fafafa" FontSize="25" Grid.Column="0" HorizontalAlignment="Center"  VerticalAlignment="Center"/>
            <Label x:Name="lblUlogovaniKorisnik" Content="Ime i prezime" Foreground="#fafafa" FontSize="16" Grid.Column="1" HorizontalAlignment="Right"  VerticalAlignment="Center"/>
            <Button x:Name="CloseButton" Style="{StaticResource CloseButtonStyle}"  VerticalAlignment="Top" HorizontalAlignment="Right" Padding="0" Margin="1" Click="CloseButton_Click" Grid.Column="2" />
        </Grid>

        <!-- Glavni grid -->
        <Grid HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Margin="10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="0.45*"/>
                <ColumnDefinition Width="0.5*"/>

            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="0.30*"/>
                <RowDefinition Height="0.6*"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!--This is the hidden helper Grid which does the resizing -->
            <Grid Visibility="Hidden">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="0.90*"/>
                    <ColumnDefinition Width="0.13*" />
                    <ColumnDefinition Width="20"/>
                </Grid.ColumnDefinitions>
                <!--This is the hidden helper Field which is used to bind to, using the "Fill" column of the helper grid-->
                <Grid Grid.Column="1" x:Name="helperField"/>
            </Grid>
            <Grid  Grid.Row="0" Grid.Column="1" Grid.ColumnSpan="2" Margin="60,60,0,0" VerticalAlignment="Top" HorizontalAlignment="Stretch">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Label Grid.Column="1" Content="Lista stavki ulaza" FontSize="24" VerticalContentAlignment="Bottom" />
                <Viewbox Stretch="Uniform" MinWidth="100" MaxWidth="160"  Margin="2" Grid.Column="2" >
                    <Button Style="{StaticResource PrimaryButtonStyle}" HorizontalAlignment="Right" BorderThickness="0" Margin="5"  x:Name="BtnPrint" Click="BtnPrint_Click"    Width="120"    Height="50"   Foreground="{DynamicResource GlobalFontColor}"   >
                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                            <TextBlock FontFamily="Segoe MDL2 Assets" FontSize="20" Margin="5" FontWeight="ExtraLight" Text="&#xE749;" Foreground="#F5F5F5" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            <TextBlock Text="Štampaj"  VerticalAlignment="Center"  Foreground="#F5F5F5" />
                        </StackPanel>
                    </Button>
                </Viewbox >
            </Grid>


            <!--  Search TextBox -->
            <StackPanel Orientation="Horizontal" Grid.Row="0"  Grid.Column="0" Margin="30,60,0,0"  >
                <Border Padding="5" Width="300" Height="50" HorizontalAlignment="Left"  BorderThickness="1" Background="Transparent"  BorderBrush="{DynamicResource GlobalFontColor}" CornerRadius="15" >
                    <DockPanel Grid.Row="1" Grid.Column="1" Margin="1" >
                        <TextBlock FontFamily="pack://application:,,,/Assets/Fonts/#codicon" Margin="5" FontSize="24" Width="30" FontWeight="ExtraLight" Text="&#60013;" Foreground="{DynamicResource GlobalFontColor}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        <TextBox x:Name="SearchTextBox" CaretBrush="{DynamicResource GlobalFontColor}"  Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" Foreground="{DynamicResource GlobalFontColor}"  FontSize="16" HorizontalAlignment="Stretch" VerticalContentAlignment="Center" Background="Transparent" BorderBrush="Transparent" Padding="5" >
                        <TextBox.Style>
                            <Style TargetType="TextBox">
                                <Style.Triggers>
                                    <Trigger Property="IsFocused" Value="True">
                                        <Setter Property="BorderBrush" Value="Transparent"/>
                                        <Setter Property="BorderThickness" Value="0"/>
                                        <Setter Property="Background" Value="Transparent"/>
                                    </Trigger>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="BorderBrush" Value="Transparent"/>
                                        <Setter Property="BorderThickness" Value="0"/>
                                        <Setter Property="Background" Value="Transparent"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </TextBox.Style>
                    </TextBox>
                </DockPanel>
                </Border>

                <Button Style="{StaticResource SecondaryButtonStyle}" x:Name="BtnFirst" Click="BtnFirst_Click"   MinWidth="50" Height="50" VerticalAlignment="Center"   Grid.Column="0" HorizontalAlignment="Center" ToolTip="Prvi artikl" Margin="10,0,0,0">
                    <TextBlock 
                       FontFamily="pack://application:,,,/Assets/Fonts/#codicon"
                       FontSize="24"
                       FontWeight="ExtraLight"
                       Text="&#60271;" 
                       Foreground="{DynamicResource GlobalFontColor}"
                       HorizontalAlignment="Center"
                       VerticalAlignment="Center"/>
                </Button>
                <Button Style="{StaticResource SecondaryButtonStyle}"  x:Name="BtnLast" Click="BtnLast_Click" MinWidth="50" Height="50" Padding="0"  VerticalAlignment="Center"  Grid.Column="2" ToolTip="Naredni artikl" Margin="10,0,0,0" >
                    <TextBlock 
                      FontFamily="pack://application:,,,/Assets/Fonts/#codicon"
                      FontSize="24"
                      FontWeight="ExtraLight"
                      Text="&#60272;" 
                      Foreground="{DynamicResource GlobalFontColor}"
                      HorizontalAlignment="Center"
                      VerticalAlignment="Center"/>
                </Button>
                <Button Style="{StaticResource SecondaryButtonStyle}"  x:Name="BtnNew" Command="{Binding AddNewStockInCommand}" MinWidth="50" Height="50" Padding="0"  VerticalAlignment="Center"  Grid.Column="2" ToolTip="Naredni artikl" Margin="10,0,0,0" >
                    <TextBlock 
                        FontFamily="pack://application:,,,/Assets/Fonts/#codicon"
                        FontSize="24"
                        FontWeight="ExtraLight"
                        Text="&#60000;" 
                        Foreground="{DynamicResource GlobalFontColor}"
                        HorizontalAlignment="Center"
                        VerticalAlignment="Center"/>
                </Button>
                <Button Style="{StaticResource SecondaryButtonStyle}"  x:Name="BtnSave" Command="{Binding SaveStockInCommand}" MinWidth="50" Height="50" Padding="0"  VerticalAlignment="Center"  Grid.Column="2" ToolTip="Naredni artikl" Margin="10,0,0,0" >
                    <TextBlock 
                              FontFamily="pack://application:,,,/Assets/Fonts/#codicon"
                              FontSize="24"
                              FontWeight="ExtraLight"
                              Text="&#xEB4B;" 
                              Foreground="{DynamicResource GlobalFontColor}"
                              HorizontalAlignment="Center"
                              VerticalAlignment="Center"/>
                </Button>


            </StackPanel>
            <Border Grid.Column="0" Grid.Row="1" BorderBrush="{DynamicResource GlobalFontColor}" BorderThickness="1" Padding="5" CornerRadius="20" Margin="20,10" MaxWidth="650" MinWidth="500" HorizontalAlignment="Left">
                <Grid Margin="10" Grid.Column="0" Grid.Row="1">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="37"/>
                        <RowDefinition Height="37"/>
                        <RowDefinition Height="37"/>
                        <RowDefinition Height="37"/>
                        <RowDefinition Height="37"/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="120"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <!-- BrojUlaza -->
                    <Label Foreground="{DynamicResource GlobalFontColor}" Grid.Row="0" Grid.Column="0" Content="Broj ulaza:" VerticalAlignment="Center"/>
                    <TextBox Style="{DynamicResource TextBoxCustom}"  Foreground="{DynamicResource GlobalFontColor}"  Grid.Row="0" Grid.Column="1" Text="{Binding SelectedStockIn.BrojUlaza, Mode=TwoWay}" Width="120" HorizontalAlignment="Left" Margin="0,0,0,5" BorderThickness="0,0,0,1" />

                    <!-- Datum -->
                    <Label Foreground="{DynamicResource GlobalFontColor}"  Grid.Row="1" Grid.Column="0" Content="Datum:" VerticalAlignment="Center"/>
                    <Border Grid.Row="1" Grid.Column="1"  BorderThickness="0,0,0,1" CornerRadius="0" Height="35" VerticalAlignment="Center"  Width="150" HorizontalAlignment="Left" Padding="0" Margin="1" >
                        <Border.Style>
                            <Style TargetType="{x:Type Border}">
                                <Setter Property="BorderBrush" Value="{DynamicResource GlobalFontColor}" />
                                <Setter Property="Background" Value="Transparent"/>
                                <Style.Triggers>
                                    <!-- Trigger za MouseOver -->
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="BorderBrush" Value="Gray"/>
                                        <Setter Property="Background" Value="Transparent"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Border.Style>
                        <DatePicker Focusable="False"   x:Name="DpDate"  CalendarStyle="{StaticResource styleCalendar}"   SelectedDate="{Binding SelectedStockIn.Datum, Mode=TwoWay}"  HorizontalAlignment="Left"  Width="150"  BorderThickness="0"  Background="Transparent" Foreground="{DynamicResource GlobalFontColor}"   HorizontalContentAlignment="Left" VerticalAlignment="Center" Padding="5">
                            <DatePicker.Resources>
                                <Style TargetType="DatePickerTextBox">
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="DatePickerTextBox">
                                                <ScrollViewer x:Name="PART_ContentHost"
            Background="Transparent"
            Focusable="False"/>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                    <Setter Property="Background" Value="Transparent"/>
                                    <Setter Property="BorderThickness" Value="0"/>
                                    <Setter Property="Padding" Value="0"/>
                                    <Setter Property="Text">
                                        <Setter.Value>
                                            <Binding Path="SelectedDate" 
                                 RelativeSource="{RelativeSource AncestorType=DatePicker}"
                                 StringFormat="dd.MM.yyyy"/>
                                        </Setter.Value>
                                    </Setter>
                                </Style>

                                <Style TargetType="DatePicker">
                                    <Setter Property="Background" Value="Transparent"/>
                                    <Setter Property="BorderThickness" Value="0"/>
                                    <Setter Property="BorderBrush" Value="Transparent"/>
                                </Style>
                            </DatePicker.Resources>
                        </DatePicker>

                    </Border>


                    <!-- Dobavljač -->
                    <Label Foreground="{DynamicResource GlobalFontColor}"  Grid.Row="2" Grid.Column="0" Content="Dobavljač:" VerticalAlignment="Center"/>
                    <ComboBox Foreground="{DynamicResource GlobalFontColor}"  Grid.Row="2" Grid.Column="1" Width="200" HorizontalAlignment="Left" Margin="0,0,0,5"
                  ItemsSource="{Binding Suppliers}" 
                  DisplayMemberPath="Dobavljac"
                  SelectedValuePath="Dobavljac"
                  SelectedValue="{Binding SelectedStockIn.Dobavljac, Mode=TwoWay}"
                   />

                    <!-- Broj fakture -->
                    <Label Foreground="{DynamicResource GlobalFontColor}"  Grid.Row="3" Grid.Column="0" Content="Broj fakture:" VerticalAlignment="Center"/>
                    <TextBox Style="{DynamicResource TextBoxCustom}"  Foreground="{DynamicResource GlobalFontColor}"  Grid.Row="3" Grid.Column="1" Text="{Binding SelectedStockIn.BrojFakture, Mode=TwoWay}" Width="150" HorizontalAlignment="Left" Margin="0,0,0,5" BorderThickness="0,0,0,1" />

                    <!-- Radnik -->
                    <Label Foreground="{DynamicResource GlobalFontColor}"  Grid.Row="4" Grid.Column="0" Content="Radnik:" VerticalAlignment="Center"/>
                    <TextBox Style="{DynamicResource TextBoxCustom}"  Foreground="{DynamicResource GlobalFontColor}"  Grid.Row="4" Grid.Column="1" Text="{Binding SelectedStockIn.RadnikName, Mode=TwoWay}" Width="200" HorizontalAlignment="Left" Margin="0,0,0,5" BorderThickness="0,0,0,1"/>
                </Grid>
            </Border>
            <Grid Grid.Row="2" Grid.RowSpan="2" Grid.Column="0">
                <Grid.RowDefinitions >
                    <RowDefinition Height="Auto"/>
                    <!-- Search -->
                    <RowDefinition Height="*"/>
                    <!-- ListView -->
                </Grid.RowDefinitions>

            <Border Padding="5" Width="300" Height="50" HorizontalAlignment="Left"  BorderThickness="1" Background="Transparent"  BorderBrush="{DynamicResource GlobalFontColor}" CornerRadius="15" Margin="20,0,0,0" Grid.Row="0">
                <DockPanel  Margin="1" >
                    <TextBlock FontFamily="pack://application:,,,/Assets/Fonts/#codicon" Margin="5" FontSize="24" Width="30" FontWeight="ExtraLight" Text="&#60013;" Foreground="{DynamicResource GlobalFontColor}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    <TextBox x:Name="SearchArticleTextBox" CaretBrush="{DynamicResource GlobalFontColor}"  Text="{Binding SearchArticleText, UpdateSourceTrigger=PropertyChanged}" Foreground="{DynamicResource GlobalFontColor}"  FontSize="16" HorizontalAlignment="Stretch" VerticalContentAlignment="Center" Background="Transparent" BorderBrush="Transparent" Padding="5" >
                        <TextBox.Style>
                            <Style TargetType="TextBox">
                                <Style.Triggers>
                                    <Trigger Property="IsFocused" Value="True">
                                        <Setter Property="BorderBrush" Value="Transparent"/>
                                        <Setter Property="BorderThickness" Value="0"/>
                                        <Setter Property="Background" Value="Transparent"/>
                                    </Trigger>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="BorderBrush" Value="Transparent"/>
                                        <Setter Property="BorderThickness" Value="0"/>
                                        <Setter Property="Background" Value="Transparent"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </TextBox.Style>
                    </TextBox>
                </DockPanel>
            </Border>
                <Label Content="Namirnice" HorizontalContentAlignment="Left" HorizontalAlignment="Right" FontSize="24" Width="300" VerticalContentAlignment="Bottom"/>
              
                <DataGrid  x:Name="ListaArikala" Margin="20,0"
                           ItemsSource="{Binding ArtikliFilter}"
                           SelectedItem="{Binding SelectedArticle, Mode=TwoWay}"
                           MouseUp="ListaArikala_MouseDoubleClick"
                           Foreground="{DynamicResource GlobalFontColor}"
                            Padding="2" 
                            GridLinesVisibility="None"
                             AutoGenerateColumns="False"
                           Background="Transparent"
                         HeadersVisibility="Column"
                         CanUserAddRows="False"
                         CanUserResizeRows="False"
                         CanUserSortColumns="False"
                         HorizontalScrollBarVisibility="Disabled"
                         BorderThickness="0"
                         ClipToBounds="True"
                         BorderBrush="{DynamicResource GlobalFontColor}"
                         SelectionMode="Single"
                           ScrollViewer.VerticalScrollBarVisibility="Auto"  
                           Grid.Row="1">
                    <DataGrid.Resources>
                        <Style TargetType="ScrollBar" BasedOn="{StaticResource WideVerticalScrollBar}"/>
                    </DataGrid.Resources>
                    <DataGrid.Columns>
                        <DataGridTemplateColumn Header="Namirnica" Width="2*" >
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding Repromaterijal}" Height="60" TextAlignment="Left" TextWrapping="Wrap" Padding="10"/>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>

                        <DataGridTemplateColumn Header="Planska cijena" Width="*">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock Text ="{Binding PlanskaCijena, StringFormat={}{0:F2}}"      TextWrapping="Wrap"       TextAlignment="Center"/>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>

                        <DataGridTemplateColumn Header="Nabavna cijena" Width="*">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock Text ="{Binding JedinicaMjereName}"         TextAlignment="Center"/>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
            </Grid>


            <ListView  x:Name="ListaStavki" Focusable="False"  ItemsSource="{Binding StockInItems}"  SelectedItem="{Binding  SelectedStockInItem, Mode=TwoWay}" Foreground="{DynamicResource GlobalFontColor}" MouseUp="ListaStavki_MouseDoubleClick" Background="Transparent" HorizontalAlignment="Stretch"     VerticalAlignment="Stretch" BorderThickness="0" Grid.Row="1"  Grid.Column="2" Grid.RowSpan="2" SelectionMode="Single"  BorderBrush="#AA949494" Margin="1" ScrollViewer.VerticalScrollBarVisibility="Visible" ScrollViewer.HorizontalScrollBarVisibility="Disabled" >
                <ListView.Resources>
                    <Style TargetType="ScrollBar" BasedOn="{StaticResource WideVerticalScrollBar}"/>
                </ListView.Resources>
                <ListView.View>
                        <GridView    >

                            <GridViewColumn x:Name="col2"  Header="Artikl"   Width="200" >
                                <GridViewColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock Text ="{Binding Artikl}" TextWrapping="Wrap" HorizontalAlignment="Left" />
                                    </DataTemplate>
                                </GridViewColumn.CellTemplate>
                            </GridViewColumn>

                        <GridViewColumn Header="Kol" Width="{Binding ElementName=helperField, Path=ActualWidth}" >
                                <GridViewColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock Text ="{Binding Kolicina, StringFormat={}{0:0.00}}" HorizontalAlignment="Right"  TextAlignment="Right" />
                                    </DataTemplate>
                                </GridViewColumn.CellTemplate>
                            </GridViewColumn>
                        <GridViewColumn Width="{Binding ElementName=helperField, Path=ActualWidth}">
                            <GridViewColumn.HeaderTemplate>
                                <DataTemplate>
                                    <TextBlock Text="Nab. cijena bez PDV" HorizontalAlignment="Center" TextAlignment="Center" TextWrapping="Wrap"/>
                                </DataTemplate>
                            </GridViewColumn.HeaderTemplate>
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock Text ="{Binding CijenaBezUPDV, StringFormat={}{0:0.00}}" TextAlignment="Right" HorizontalAlignment="Stretch"  />
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>
                        <GridViewColumn Width="{Binding ElementName=helperField, Path=ActualWidth}">
                           
                            <GridViewColumn.HeaderTemplate>
                                <DataTemplate>
                                    <TextBlock Text="Nab. cijena sa PDV" HorizontalAlignment="Center" TextAlignment="Center" TextWrapping="Wrap" />
                                </DataTemplate>
                            </GridViewColumn.HeaderTemplate>
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock Text ="{Binding CijenaSaUPDV, StringFormat={}{0:0.00}}" TextAlignment="Right" HorizontalAlignment="Stretch"  />
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>
                   
                        <GridViewColumn Header="Iznos PDV" Width="{Binding ElementName=helperField, Path=ActualWidth}" >
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock Text ="{Binding IznosUPDVa, StringFormat={}{0:0.00}}" TextAlignment="Right" HorizontalAlignment="Stretch"  />
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>
                        <GridViewColumn Header="Stopa %" Width="{Binding ElementName=helperField, Path=ActualWidth}" >
                                <GridViewColumn.CellTemplate>
                                    <DataTemplate  >
                                        <TextBlock Text ="{Binding PoreskaStopaPostotak}" HorizontalAlignment="Stretch" TextWrapping="Wrap" TextAlignment="Center"  />
                                    </DataTemplate>
                                </GridViewColumn.CellTemplate>
                            </GridViewColumn>
                        <!-- Dugme za brisanje -->
                        <GridViewColumn Header="#" Width="{Binding ElementName=helperField, Path=ActualWidth}">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                       
                                    <Button Style="{StaticResource CancelButtonStyle}"
                                            BorderBrush="Transparent"
                                            x:Name="BtnDelete" 
                                            Command="{Binding DataContext.DeleteArticleCommand,   RelativeSource={RelativeSource AncestorType=ListView}}"
                                            CommandParameter="{Binding}" Width="30" Height="30" Padding="0"  VerticalAlignment="Center"   ToolTip="Obriši stavku" >
                                        <TextBlock 
                                          FontFamily="pack://application:,,,/Assets/Fonts/#codicon"
                                          FontSize="24"
                                          FontWeight="ExtraLight"
                                          Text="&#60033;" 
                                          Foreground="{DynamicResource GlobalFontColor}" 
                                          HorizontalAlignment="Center"
                                          VerticalAlignment="Center"/>
                                    </Button>
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>
                    </GridView>
                    </ListView.View>
                </ListView>

        

         
        </Grid>
    </Grid>
</UserControl>
