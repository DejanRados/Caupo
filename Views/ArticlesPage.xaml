<UserControl x:Class="Caupo.Views.ArticlesPage"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:system="clr-namespace:System;assembly=mscorlib"
        xmlns:local="clr-namespace:Caupo.ViewModels"
        xmlns:loc="clr-namespace:Caupo.Helpers"
         xmlns:syncfusionskin ="clr-namespace:Syncfusion.SfSkinManager;assembly=Syncfusion.SfSkinManager.WPF" 
        mc:Ignorable="d"
        UseLayoutRounding="True">
  

    <Grid>
        <!-- Header -->
        <Grid HorizontalAlignment="Stretch" VerticalAlignment="Top" Height="55" Background="#323232" >
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="0.2*"/>
            </Grid.ColumnDefinitions>
            <Image Source="pack://application:,,,/Images/dsoftlogosmall.png" Width="50" Height="50" Grid.Column="0" HorizontalAlignment="Left" Margin="20,0,0,0"/>
            <Label x:Name="lblApp" Content="Caupo - Artikli" Foreground="#fafafa" FontSize="25" Grid.Column="0" HorizontalAlignment="Center"  VerticalAlignment="Center"/>
            <Label x:Name="lblUlogovaniKorisnik" Content="Ime i prezime" Foreground="#fafafa" FontSize="16" Grid.Column="1" HorizontalAlignment="Right"  VerticalAlignment="Center"/>
            <Button x:Name="CloseButton" Style="{StaticResource CloseButtonStyle}"  VerticalAlignment="Top" HorizontalAlignment="Right" Padding="0" Margin="1" Click="CloseButton_Click" Grid.Column="2" />
        </Grid>

        <!-- Glavni grid -->
        <Grid HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="0.4*"/>
                <ColumnDefinition Width="0.5*"/>

            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="0.25*"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="0.2*"/>
            </Grid.RowDefinitions>

            <Grid  Grid.Row="0" Grid.Column="1" Grid.ColumnSpan="2" Margin="10,60,60,0" HorizontalAlignment="Right" VerticalAlignment="Center">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Viewbox Stretch="Uniform" MinWidth="100" MaxWidth="160"  Margin="2" Grid.Column="0">
                    <Button Style="{StaticResource PrimaryButtonStyle}"   Margin="5"     x:Name="BtnEdit"     Width="120"    Height="50"      Click="BtnEdit_Click" >
                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center" >
                            <TextBlock FontFamily="pack://application:,,,/Assets/Fonts/#codicon" Margin="6" FontSize="24" Width="30" FontWeight="ExtraLight" Text="&#60019;" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            <TextBlock Text="Izmjeni"  VerticalAlignment="Center"   />
                        </StackPanel>
                    </Button>
                </Viewbox>
                <Viewbox Stretch="Uniform" MinWidth="100" MaxWidth="160"  Margin="2" Grid.Column="1">
                    <Button Style="{StaticResource PrimaryButtonStyle}"   Margin="5"  x:Name="BtnAdd"     Width="120"    Height="50"       Click="BtnAdd_Click">
                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                            <TextBlock FontFamily="pack://application:,,,/Assets/Fonts/#codicon" Margin="6" FontSize="24" Width="30" FontWeight="ExtraLight" Text="&#60000;"  HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            <TextBlock Text="Dodaj"  VerticalAlignment="Center"   />
                        </StackPanel>
                    </Button>
                </Viewbox>
                <Viewbox Stretch="Uniform" MinWidth="100" MaxWidth="160"  Margin="2" Grid.Column="2">
                    <Button Style="{StaticResource PrimaryButtonStyle}"  Margin="5"  x:Name="BtnDelete"    Width="120"    Height="50"        Click="BtnDelete_Click">
                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                            <TextBlock FontFamily="pack://application:,,,/Assets/Fonts/#codicon" Margin="6" FontSize="24" Width="30" FontWeight="ExtraLight" Text="&#60033;"  HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            <TextBlock Text="Obriši"  VerticalAlignment="Center"   />
                        </StackPanel>
                    </Button>

                </Viewbox>
                <Viewbox Stretch="Uniform" MinWidth="100" MaxWidth="160"  Margin="2" Grid.Column="3">
                    <Button Style="{StaticResource PrimaryButtonStyle}" Margin="5" x:Name="BtnExport"    Width="120"    Height="50"         Click="BtnExport_Click">
                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                            <TextBlock FontFamily="pack://application:,,,/Assets/Fonts/#codicon" Margin="6" FontSize="24" Width="30" FontWeight="ExtraLight" Text="&#xec28;"  HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            <TextBlock Text="Izvoz"  VerticalAlignment="Center"   />
                        </StackPanel>
                    </Button>
                </Viewbox>
                <Viewbox Stretch="Uniform" MinWidth="100" MaxWidth="160"  Margin="2" Grid.Column="4">
                    <Button Style="{StaticResource PrimaryButtonStyle}"    Margin="5" x:Name="BtnImport"    Width="120"    Height="50"         Click="BtnImport_Click">
                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                            <TextBlock FontFamily="pack://application:,,,/Assets/Fonts/#codicon" Margin="6" FontSize="24" Width="30" FontWeight="ExtraLight" Text="&#xEC26;"  HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            <TextBlock Text="Uvoz"  VerticalAlignment="Center"   />
                        </StackPanel>
                    </Button>
                </Viewbox>
            

            </Grid>
        

            <!--  Search TextBox -->
            <StackPanel Orientation="Horizontal" Grid.Row="0"  Grid.Column="0" Margin="30,60,0,0"  >
                <Border Padding="5" Width="300" Height="50" HorizontalAlignment="Left"  BorderThickness="2" Background="Transparent"  BorderBrush="#774A6FA5" CornerRadius="15" >
                    <DockPanel Grid.Row="1" Grid.Column="1" Margin="1" >
                        <TextBlock FontFamily="pack://application:,,,/Assets/Fonts/#codicon" Margin="6" FontSize="24" Width="30" FontWeight="ExtraLight" Text="&#60013;" Foreground="{DynamicResource GlobalFontColor}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        <TextBox x:Name="SearchTextBox" GotFocus="TextBox_GotFocus" LostFocus="TextBox_LostFocus" CaretBrush="{DynamicResource GlobalFontColor}"  Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" Foreground="{DynamicResource GlobalFontColor}"  FontSize="16" HorizontalAlignment="Stretch" VerticalContentAlignment="Center" Background="Transparent" BorderBrush="Transparent" Padding="5" >
                            <TextBox.Style>
                                <Style TargetType="TextBox">
                                    <Style.Triggers>
                                        <Trigger Property="IsFocused" Value="True">
                                            <Setter Property="BorderBrush" Value="Transparent"/>
                                            <Setter Property="BorderThickness" Value="0"/>
                                            <Setter Property="Background" Value="Transparent"/>
                                        </Trigger>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="BorderBrush" Value="Transparent"/>
                                            <Setter Property="BorderThickness" Value="0"/>
                                            <Setter Property="Background" Value="Transparent"/>
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBox.Style>
                        </TextBox>
                    </DockPanel>
                </Border>

                <Button Style="{StaticResource SecondaryButtonStyle}"   x:Name="BtnFirst" Click="BtnFirst_Click" Width="50" Height="50" VerticalAlignment="Center"   Grid.Column="0" HorizontalAlignment="Center" ToolTip="Prvi artikl" Margin="10,0,0,0">
                     <TextBlock 
             FontFamily="pack://application:,,,/Assets/Fonts/#codicon"
             FontSize="24"
             FontWeight="ExtraLight"
             Text="&#60271;" 
             Foreground="{DynamicResource GlobalFontColor}"
             HorizontalAlignment="Center"
             VerticalAlignment="Center"/>
                </Button>
                <Button Style="{StaticResource SecondaryButtonStyle}"      x:Name="BtnLast" Click="BtnLast_Click" Width="50" Height="50" Padding="0"  VerticalAlignment="Center"  Grid.Column="2" ToolTip="Naredni artikl" Margin="10,0,0,0" >
                    <TextBlock 
            FontFamily="pack://application:,,,/Assets/Fonts/#codicon"
            FontSize="24"
            FontWeight="ExtraLight"
            Text="&#60272;" 
            Foreground="{DynamicResource GlobalFontColor}"
            HorizontalAlignment="Center"
            VerticalAlignment="Center"/>
                </Button>


            </StackPanel>
            <DataGrid x:Name="ListaArtikala" VerticalAlignment="Top" Margin="30,20"
                Grid.Row="1" Grid.RowSpan="2" Grid.Column="0"
                MaxWidth="950" MaxHeight="800"
                SelectionChanged="ListaArtikala_SelectionChanged" 
                GridLinesVisibility="None"
                ItemsSource="{Binding ArtikliFilter}"
                SelectedItem="{Binding SelectedArticle, Mode=TwoWay}" 
                AutoGenerateColumns="False"
                HeadersVisibility="Column"
                CanUserAddRows="False"
                CanUserResizeRows="False"
                CanUserSortColumns="False"
                RowHeight="35"
                HorizontalScrollBarVisibility="Disabled"
                Background="Transparent"
                BorderThickness="0"
                ClipToBounds="True"
                BorderBrush="{DynamicResource GlobalFontColor}"
                ScrollViewer.VerticalScrollBarVisibility="Auto"
                SelectionMode="Single">
             
                <DataGrid.Resources>
                    <Style TargetType="ScrollBar" BasedOn="{StaticResource WideVerticalScrollBar}"/>
                </DataGrid.Resources>
                <DataGrid.Columns>

                    <DataGridTemplateColumn Header="Artikl" Width="2*" >
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding ArtiklNormativ}" TextWrapping="Wrap" HorizontalAlignment="Left" Padding="10,0"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    <DataGridTemplateColumn Header="Cijena" Width="*" >
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding Cijena, StringFormat={}{0:F2}}"   TextAlignment="Center"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    <DataGridTemplateColumn Header="Šifra" Width="*">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding Sifra}"               TextAlignment="Center"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    <DataGridTemplateColumn Header="Vrsta artikla" Width="*">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding VrstaArtiklaName}"            TextAlignment="Center"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                </DataGrid.Columns>
            </DataGrid>

       
            <!-- Grid za dodavanje ili izmjenu -->
            <Border Grid.Row="1" Grid.Column="1" Grid.RowSpan="2" BorderBrush="{DynamicResource GlobalFontColor}" BorderThickness="1" CornerRadius="10" Margin="20" >
            <Grid Grid.Row="1" Grid.Column="1" Grid.RowSpan="2" Margin="20,5"  >
                <ScrollViewer VerticalScrollBarVisibility="Hidden"  >
                    <StackPanel>
                            <!--IdArtikl-->
                            <Grid Visibility="Collapsed" >
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="0.33*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Label  Content="Id artikla" Foreground="{DynamicResource GlobalFontColor}" VerticalAlignment="Center" Margin="10" Grid.Column="0" BorderThickness="0,0,0,1" BorderBrush="{DynamicResource GlobalFontColor}" VerticalContentAlignment="Bottom" />
                            <TextBox x:Name="IdArtiklaTextBox" Style="{DynamicResource TextBoxCustom}"  VerticalContentAlignment="Bottom" Padding="3" IsReadOnly="True"  Text ="{Binding SelectedArticle.IdArtikla}" Grid.Column="1" BorderThickness="0,0,0,1" Background="Transparent" Width="100" HorizontalAlignment="Left"/>
                        </Grid>
                        <!--Sifra-->
                        <Grid Height="35" >
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="0.33*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Label Content="Šifra" Foreground="{DynamicResource GlobalFontColor}" VerticalAlignment="Center"  Grid.Column="0"  VerticalContentAlignment="Bottom"/>
                            <TextBox x:Name="SifraTextBox" CaretBrush="{DynamicResource GlobalFontColor}" Style="{DynamicResource TextBoxCustom}"  FontSize="15" Margin="1" VerticalContentAlignment="Bottom" Padding="3" Text ="{Binding SelectedArticle.Sifra, Mode=OneWay}" Grid.Column="1" BorderThickness="0,0,0,1" Background="Transparent" Width="100" HorizontalAlignment="Left"  />
                        </Grid>
                            <!--Artikl-->
                            <Grid Height="43" >
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="0.33*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="35"/>
                                    <RowDefinition Height="8"/>
                                </Grid.RowDefinitions>
                            <Label Content ="Artikl" Foreground="{DynamicResource GlobalFontColor}" VerticalAlignment="Bottom" Grid.Column="0" Grid.Row="0" VerticalContentAlignment="Center"/>
                            <TextBox  x:Name="ArtiklTextBox"  Style="{DynamicResource TextBoxCustom}"  CaretBrush="{DynamicResource GlobalFontColor}" VerticalAlignment="Bottom"
                                     Grid.Column="1" Grid.Row="0" 
                                      GotFocus="ArtiklTextBox_GotFocus"
                                      LostFocus="ArtiklTextBox_LostFocus"
                                      TextChanged="ArtiklTextBox_TextChanged"
                                     Text = "{Binding SelectedArticle.Artikl}"
                                     FontSize="15" 
                                      Height="30"
                                     HorizontalAlignment="Left"
                                     VerticalContentAlignment="Bottom" 
                                     Background="Transparent" 
                                      BorderThickness="0,0,0,1"
                                      
                                       Padding="3"
                                      Width="250"/>
                                <Label x:Name="lblArtikl" Content="Obavezan podatak" FontSize="8" Grid.Column="1" Grid.Row="1"  VerticalAlignment="Top" Height="8" Padding="0" Foreground="red" Visibility="Collapsed"/>
                            </Grid>
                            <!--Cijena-->
                            <Grid Height="43" >
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="0.33*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="35"/>
                                    <RowDefinition Height="10"/>
                                </Grid.RowDefinitions>
                                <Label Content ="Cijena" Foreground="{DynamicResource GlobalFontColor}" VerticalAlignment="Bottom"  Grid.Column="0" Grid.Row="0" VerticalContentAlignment="Center" />
                            <TextBox x:Name="CijenaTextBox" Style="{DynamicResource TextBoxCustom}"  CaretBrush="{DynamicResource GlobalFontColor}" 
                                     FontSize="15"
                                     VerticalContentAlignment="Bottom"
                                     GotFocus="CijenaTextBox_GotFocus"
                                     LostFocus="CijenaTextBox_LostFocus"
                                     Padding="3"
                                     Text ="{Binding SelectedArticle.Cijena, StringFormat='{}{0:F2}'}"
                                     Grid.Column="1" Grid.Row="0"
                                     BorderThickness="0,0,0,1"
                                     Background="Transparent"
                                     Width="100" HorizontalAlignment="Left" />
                                <Label x:Name="lblCijena" Content="Obavezan podatak" FontSize="8" Grid.Column="1" Grid.Row="1"  VerticalAlignment="Top" Height="10" Padding="0" Foreground="red" Visibility="Collapsed"/>
                            </Grid>
                            <!--Int sifral-->
                            <Grid Height="40"  x:Name="InternaSifraGrid" Visibility="Collapsed">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="0.33*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Label Content ="Interna Šifra" Foreground="{DynamicResource GlobalFontColor}" VerticalAlignment="Center"  Grid.Column="0" VerticalContentAlignment="Bottom"/>
                            <TextBox x:Name="InternaSifraTextBox" Style="{DynamicResource TextBoxCustom}"  CaretBrush="{DynamicResource GlobalFontColor}"
                                     FontSize="15"
                                     VerticalContentAlignment="Bottom"
                                     Padding="3"
                                     Text ="{Binding SelectedArticle.InternaSifra}"
                                     Grid.Column="1" BorderThickness="0,0,0,1"
                                     Background="Transparent" Width="100"
                                     HorizontalAlignment="Left"   />
                        </Grid>
                            <!--Slika-->
                            <Grid Height="40"  x:Name="SlikaGrid" Visibility="Collapsed">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="0.33*"/>
                                <ColumnDefinition Width="*"/>

                            </Grid.ColumnDefinitions>
                            <Label Content ="Slika" Foreground="{DynamicResource GlobalFontColor}" VerticalAlignment="Center"  Grid.Column="0" VerticalContentAlignment="Bottom"/>
                            <StackPanel Orientation="Horizontal" Grid.Column="1"  >
                                <TextBox x:Name="SlikaTextBox" Style="{DynamicResource TextBoxCustom}"  CaretBrush="{DynamicResource GlobalFontColor}"
                                         FontSize="15"
                                         VerticalContentAlignment="Bottom"
                                         Padding="3"
                                         Text ="{Binding SelectedArticle.Slika}"
                                         BorderThickness="0,0,0,1"
                                         Background="Transparent"
                                         Width="250" HorizontalAlignment="Left" />

                                <Button Style="{StaticResource SecondaryButtonStyle}"  x:Name="btnSlika"    Width="50"    Height="40"   Foreground="{DynamicResource GlobalFontColor}"   Click="btnSlika_Click" Grid.Column="1" HorizontalAlignment="Left" Margin="10,0,0,0">
                                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center" HorizontalAlignment="Center">
                                        <TextBlock FontFamily="pack://application:,,,/Assets/Fonts/#codicon"  FontSize="24"  FontWeight="ExtraLight" Text="&#xEAEA;" Foreground="{DynamicResource GlobalFontColor}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </StackPanel>
                                </Button>
                            </StackPanel>
                        </Grid>
                            <!--JM-->
                            <Grid Height="40"  x:Name="JedinicaMjereGrid" Visibility="Collapsed">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="0.33*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Label Content ="Jedinica mjere" Foreground="{DynamicResource GlobalFontColor}" VerticalAlignment="Center"  Grid.Column="0" VerticalContentAlignment="Bottom" />
                            <ComboBox x:Name="JediniceMjerePicker" Foreground="{DynamicResource GlobalFontColor}" FontSize="15"  VerticalContentAlignment="Center" Padding="3"  Width="100"    HorizontalAlignment="Left" Grid.Column="1" Margin="0,5,0,0" 
                                 HorizontalContentAlignment="Left" 
                                      BorderThickness="0,0,0,1"
                                      Background="Transparent"
                                   DisplayMemberPath="JedinicaMjere"
                                  ItemsSource="{Binding JediniceMjere }"
                                  SelectedItem="{Binding SelectedJedinicaMjere}"/>
                        </Grid>
                            <!--PS-->
                            <Grid Height="40"  x:Name="PoreskeStopeGrid" Visibility="Collapsed">
                               <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="0.33*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Label Content ="Poreska stopa" Foreground="{DynamicResource GlobalFontColor}" VerticalAlignment="Center"  Grid.Column="0" VerticalContentAlignment="Bottom"/>
                            <ComboBox x:Name="PoreskeStopePicker" Foreground="{DynamicResource GlobalFontColor}"  Width="200"   FontSize="15"    HorizontalAlignment="Left"  Grid.Column="1" Margin="0,5,0,0" VerticalContentAlignment="Center" Padding="3"
                                   HorizontalContentAlignment="Left" SelectedItem="{Binding SelectedPoreskaStopa}"
                                            BorderThickness="0,0,0,1"
                                     Background="Transparent"
                                   ItemsSource="{Binding PoreskeStope }"
                                   DisplayMemberPath="Opis" />
                        </Grid>
                            <!--Normativ-->
                            <Grid Height="40"  x:Name="NormativGrid" Visibility="Collapsed">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="0.33*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Label Content ="Normativ pića" Foreground="{DynamicResource GlobalFontColor}" VerticalAlignment="Center"  Grid.Column="0" VerticalContentAlignment="Bottom"/>
                            <ComboBox x:Name="NormativPicker" Foreground="{DynamicResource GlobalFontColor}" Width="100"  FontSize="15"   HorizontalAlignment="Left" Grid.Column="1"  Margin="0,5,0,0" VerticalContentAlignment="Center" Padding="3"
                                          HorizontalContentAlignment="Left" SelectedItem="{Binding SelectedNormativ}"
                                            BorderThickness="0,0,0,1"
                                        Background="Transparent"
                                        ItemsSource="{Binding Normativi}"
                                        DisplayMemberPath="Normativ"
                                       SelectionChanged="NormativPicker_SelectionChanged" />
                        </Grid>
                            <!--Vrsta-->
                            <Grid Height="40"  x:Name="VrstaArtiklaGrid" Visibility="Collapsed">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="0.33*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Label Content ="Vrsta artikla" Foreground="{DynamicResource GlobalFontColor}" VerticalAlignment="Center"  Grid.Column="0" VerticalContentAlignment="Bottom"/>
                            <ComboBox x:Name="VrstaArtiklaPicker" Foreground="{DynamicResource GlobalFontColor}" Width="200"  FontSize="15"       BorderThickness="0,0,0,1"   Background="Transparent"  HorizontalAlignment="Left"    HorizontalContentAlignment="Left" SelectedItem="{Binding SelectedVrstaArtikla}" Grid.Column="1"  Margin="0,5,0,0" VerticalContentAlignment="Center" Padding="3">
                                <system:String>Piće</system:String>
                                <system:String>Hrana</system:String>
                                <system:String>Ostalo</system:String>
                            </ComboBox>
                        </Grid>
                            <!--Kategorijal-->
                            <Grid Height="40"  x:Name="KategorijaGrid" Visibility="Collapsed">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="0.33*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Label Content ="Kategorija" Foreground="{DynamicResource GlobalFontColor}" VerticalAlignment="Center"  Grid.Column="0" VerticalContentAlignment="Bottom"/>
                            <StackPanel Orientation="Horizontal" Grid.Column="1" >
                                <ComboBox x:Name="KategorijePicker"  Width="250"  Foreground="{DynamicResource GlobalFontColor}" FontSize="15"   HorizontalAlignment="Left" Grid.Column="1"  Margin="0,5,0,0" VerticalContentAlignment="Center" Padding="3"
                                                   HorizontalContentAlignment="Left" SelectedItem="{Binding SelectedKategorija, Mode=TwoWay}"       BorderThickness="0,0,0,1"
                                                    Background="Transparent"
                                                  ItemsSource="{Binding Kategorije}"
                                                  DisplayMemberPath="Kategorija" />
                                <Button Style="{StaticResource SecondaryButtonStyle}"  x:Name="btnKategorija"    Width="50"    Height="40"   Foreground="{DynamicResource GlobalFontColor}"   Click="btnKategorija_Click" Grid.Column="1" HorizontalAlignment="Left" Margin="10,0,0,0">
                                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center" HorizontalAlignment="Center">
                                        <TextBlock FontFamily="Segoe MDL2 Assets"  FontSize="24"  FontWeight="ExtraLight" Text="&#xED0E;" Foreground="{DynamicResource GlobalFontColor}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </StackPanel>
                                </Button>
                            </StackPanel>
                        </Grid>
                            <!--Pozicija-->
                            <Grid Height="40"  x:Name="PozicijaGrid" Visibility="Collapsed">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="0.33*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Label Content ="Pozicija" Foreground="{DynamicResource GlobalFontColor}" VerticalAlignment="Center"  Grid.Column="0" VerticalContentAlignment="Bottom"/>
                            <TextBox x:Name="PozicijaTextBox" Style="{DynamicResource TextBoxCustom}"  CaretBrush="{DynamicResource GlobalFontColor}" Foreground="{DynamicResource GlobalFontColor}"
                                     FontSize="15"
                                     VerticalContentAlignment="Bottom"
                                     Padding="3"
                                     Text ="{Binding SelectedArticle.Pozicija}" 
                                     Grid.Column="1" 
                                     Width="100" 
                                     HorizontalAlignment="Left"
                                     BorderThickness="0,0,0,1" Background="Transparent" />
                        </Grid>
                            <!--ArtiklNorm-->
                            <Grid Height="40"  x:Name="ArtiklNormativGrid" Visibility="Collapsed">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="0.33*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Label Content ="Artikl Normativ" Foreground="{DynamicResource GlobalFontColor}" VerticalAlignment="Center"  Grid.Column="0" VerticalContentAlignment="Bottom"/>
                            <TextBox x:Name="ArtiklNormativTextBox" Style="{DynamicResource TextBoxCustom}"  CaretBrush="{DynamicResource GlobalFontColor}" Foreground="{DynamicResource GlobalFontColor}"
                                     FontSize="15"  
                                     VerticalContentAlignment="Bottom"
                                     Padding="3"
                                     IsReadOnly="True" 
                                     Text="{Binding SelectedArticle.ArtiklNormativ}"
                                     Grid.Column="1" 
                                     Width="200" 
                                     HorizontalAlignment="Left"
                                     BorderThickness="0,0,0,1" Background="Transparent"   />
                        </Grid>
                            <!--Prikaz-->
                            <Grid Height="40"  x:Name="PrikazGrid" Visibility="Collapsed">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="0.33*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Label Content ="Prikaz na displeju" Foreground="{DynamicResource GlobalFontColor}" VerticalAlignment="Center"  Grid.Column="0" VerticalContentAlignment="Bottom"/>
                            <ComboBox x:Name="PrikazatiNaDisplejuPicker" FontSize="15"  Width="150"   Foreground="{DynamicResource GlobalFontColor}"      BorderThickness="0,0,0,1"  Background="Transparent"  HorizontalAlignment="Left"      HorizontalContentAlignment="Left" SelectedItem="{Binding SelectedArticle.PrikazatiNaDispleju}" Grid.Column="1"  Margin="0,5,0,0" VerticalContentAlignment="Center" Padding="3">
                                <system:String>DA</system:String>
                                <system:String>NE</system:String>
                            </ComboBox>
                        </Grid>
                            <!--EditGrid-->
                            <Grid x:Name="EditGrid" Visibility="Collapsed"  Margin=" 5" >
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="0.3*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Button Style="{StaticResource CancelButtonStyle}"  x:Name="CancelEditButton" Margin="10,0"   Width="120"    Height="50"    Click="CancelEditButton_Click" Grid.Column="0" HorizontalAlignment="Right">
                                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                    <TextBlock FontFamily="pack://application:,,,/Assets/Fonts/#codicon"  FontSize="24" Width="30" FontWeight="ExtraLight" Text="&#60039;" Foreground="{DynamicResource GlobalFontColor}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    <TextBlock Text="Odustani"  VerticalAlignment="Center"  Foreground="{DynamicResource GlobalFontColor}" />
                                </StackPanel>
                            </Button>

                            <Button  Style="{StaticResource PrimaryButtonStyle}"  x:Name="SaveEditButton"    Width="120"    Height="50"    Click="SaveEditButton_Click" Grid.Column="1" HorizontalAlignment="Left">
                                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                    <TextBlock FontFamily="pack://application:,,,/Assets/Fonts/#codicon"  FontSize="24" Width="30" FontWeight="ExtraLight" Text="&#xEB4B;"  HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    <TextBlock Text="Spremi"  VerticalAlignment="Center"   />
                                </StackPanel>
                            </Button>

                        </Grid>
                            <!--AddGrid-->
                            <Grid x:Name="AddGrid" Visibility="Collapsed"  Margin="5">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="0.33*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Button Style="{StaticResource CancelButtonStyle}"   x:Name="CancelAddButton" Margin="10,0"   Width="120"    Height="50"   Foreground="{DynamicResource GlobalFontColor}"      Click="CancelAddButton_Click" Grid.Column="0" HorizontalAlignment="Right">
                                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                    <TextBlock FontFamily="pack://application:,,,/Assets/Fonts/#codicon"  FontSize="24" Width="30" FontWeight="ExtraLight" Text="&#60039;" Foreground="{DynamicResource GlobalFontColor}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    <TextBlock Text="Odustani"  VerticalAlignment="Center"  Foreground="{DynamicResource GlobalFontColor}" />
                                </StackPanel>
                            </Button>

                            <Button  Style="{StaticResource PrimaryButtonStyle}"    x:Name="SaveAddButton"    Width="120"    Height="50"   Foreground="{DynamicResource GlobalFontColor}"      Click="SaveAddButton_Click" Grid.Column="1" HorizontalAlignment="Left">
                                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                    <TextBlock FontFamily="pack://application:,,,/Assets/Fonts/#codicon"  FontSize="24" Width="30" FontWeight="ExtraLight" Text="&#xEB4B;" Foreground="{DynamicResource GlobalFontColor}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    <TextBlock Text="Spremi"  VerticalAlignment="Center"  Foreground="{DynamicResource GlobalFontColor}" />
                                </StackPanel>
                            </Button>
                        </Grid>
                    </StackPanel>
                </ScrollViewer>
            </Grid>
            </Border>

        </Grid>
    </Grid>
</UserControl>
